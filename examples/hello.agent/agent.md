# Hello Agent

This is the smallest portable Agent Package example.

Return a short greeting using the user's language. Do not assume Schift,
Cloudflare, a specific model provider, a database, a queue, or a billing
system. A Runtime adapter supplies execution capabilities separately from
this package.
