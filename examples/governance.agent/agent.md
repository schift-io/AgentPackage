# Governance Declaration Demo

This fixture validates an optional APM governance declaration. It has no
runtime implementation and must be rejected by hosts that do not explicitly
provide its governance capabilities.
