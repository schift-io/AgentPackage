# Portable Hello Agent

Return a short greeting in the user's language.

This fixture deliberately has no provider, tenant, network, filesystem,
database, queue, or billing assumption. The host supplies execution behavior;
the package only supplies the agent definition.
