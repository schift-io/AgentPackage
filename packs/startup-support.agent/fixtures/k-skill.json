{
  "name": "k-startup-support",
  "description": "K-skill that fetches, uploads, and retrieves Korean startup-support public notices and user-uploaded PDF/TXT/MD/HWPX documents.",
  "bucket_name": "startup-support-demo",
  "skill_file": "skills/startup-support.md",
  "source_registry": "sources.json",
  "source_urls": "source_urls.txt",
  "fetch_script": "scripts/fetch-sources.mjs",
  "upload_script": "scripts/upload-folder.mjs",
  "register_script": "scripts/register-k-skill.mjs",
  "bound_bucket_env": "SCHIFT_BUCKET_ID",
  "api_paths": {
    "create_bucket": "POST /v1/buckets",
    "upload": "POST /v1/buckets/{bucket_id}/upload",
    "register_skill": "POST /v1/skills",
    "update_skill": "PATCH /v1/skills/{skill_id}",
    "invoke_skill": "POST /v1/skills/{skill_id}/invoke"
  },
  "principle": "The skill carries source-fetching instructions and source registry files. The RAG answer must come from uploaded documents in the bound bucket."
}

